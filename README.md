  Final Instructions for Use:
   1. On any Linux server, clone the repo:
      git clone git@github.com:inextor/ScoreImages.git
   2. Build it once (requires cargo installed):
      cd ScoreImages/sharp-score && cargo build --release
   3. Run it anywhere:
      ./target/release/sharp-score my_photo.jpg
